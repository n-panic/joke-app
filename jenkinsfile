pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "echo 'Building...'"
                sh 'npm install'
                sh 'npm run build'
                sh "echo 'Build complete'"
            }
        }
    }
}