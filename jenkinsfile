pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "echo 'Building...'"
                script {
                    sh 'npm install'
                    sh 'npm run build'
                }
                sh "echo 'Build complete'"
            }
            
        }
    }
}