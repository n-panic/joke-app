pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "echo 'Building...'"
                script {
                    sh 'npm install -g pnpm'
                    sh 'pnpm install'
                    sh 'pnpm run build'
                }
                sh "echo 'Build complete'"
            }
            
        }
    }
}